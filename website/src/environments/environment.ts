export const environment = {
  production: false,
  apiUrl: '../../data',
  version: '1.0.0',
  // Increment this version when deploying new images to force browser cache refresh
  cacheVersion: '1.0.1'
};